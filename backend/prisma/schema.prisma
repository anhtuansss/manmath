generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

  model MathProblem {
  id             Int      @id @default(autoincrement())
  question       String
  choice         String[]
  answer         String
  createdAt      DateTime @default(now())
}